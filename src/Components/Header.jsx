import logo from "../assets/Iteration-1-assets/logo.svg";

const Header = () => {
  return (
    <header class="header">
      <img src={logo} alt="hansel" />
    </header>
  );
};
export default Header;
